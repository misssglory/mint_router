[package]
name = "tcp_router"
version = "0.1.0"
edition = "2021"
default-run = "tcp_router"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
chrono = "0.4"
regex = "1.10"

[dependencies.tokio]
version = "1.0"
features = ["full"]
